# Purpose: Execute comprehensive test suite covering backend, frontend, and integration tests
# Scope: All pull requests and pushes to main branch for code quality assurance
# Overview: This workflow runs the complete test suite using Docker-cached environments for
#     optimal performance. It executes backend tests with pytest coverage, frontend tests with
#     Jest/Vitest coverage, integration tests, and design linter framework tests. Coverage
#     reports are uploaded as artifacts and test summaries are generated for visibility.
name: Testing

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main]

env:
  PYTHON_VERSION: '3.11'
  NODE_VERSION: '20'
  DOCKER_BUILDKIT: 1
  COMPOSE_DOCKER_CLI_BUILD: 1

jobs:
  test:
    name: Test Suite
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        if: ${{ !env.ACT }}
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      # Set up Docker Buildx for advanced caching features
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Cache Docker layers between builds
      - name: Cache Docker layers
        uses: actions/cache@v4
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ hashFiles('**/Dockerfile*', '**/package*.json', '**/pyproject.toml', '**/poetry.lock') }}
          restore-keys: |
            ${{ runner.os }}-buildx-

      # Build images with cache
      - name: Build Docker images with cache
        run: |
          export BUILDX_CACHE_FROM="type=local,src=/tmp/.buildx-cache"
          export BUILDX_CACHE_TO="type=local,dest=/tmp/.buildx-cache-new,mode=max"

          docker buildx build \
            --cache-from $BUILDX_CACHE_FROM \
            --cache-to $BUILDX_CACHE_TO \
            --load \
            -t durable-code-backend-dev:latest \
            -f ./durable-code-app/backend/Dockerfile.dev \
            ./durable-code-app/backend

          docker buildx build \
            --cache-from $BUILDX_CACHE_FROM \
            --cache-to $BUILDX_CACHE_TO \
            --load \
            -t durable-code-frontend-dev:latest \
            -f ./durable-code-app/frontend/Dockerfile.dev \
            ./durable-code-app/frontend

      # Move cache for next run
      - name: Move cache
        run: |
          rm -rf /tmp/.buildx-cache
          mv /tmp/.buildx-cache-new /tmp/.buildx-cache

      # Run tests using existing make target
      - name: Run comprehensive test suite
        run: make test-all

      - name: Upload coverage reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-reports
          path: |
            **/coverage.xml
            **/coverage/
            **/htmlcov/
          retention-days: 30

      - name: Generate test summary
        if: always()
        run: |
          echo "## 🧪 Test Summary" >> $GITHUB_STEP_SUMMARY
          echo "All tests completed using \`make test-all\`" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Backend tests with coverage (pytest)" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Frontend tests with coverage (Jest/Vitest)" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Integration tests" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Design linter framework tests" >> $GITHUB_STEP_SUMMARY
