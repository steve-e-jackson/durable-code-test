# =============================================================================
# AUTHENTICATION & API KEYS
# =============================================================================

# Claude API Configuration
# Get from: https://console.anthropic.com/
CLAUDE_API_KEY=your-claude-api-key-here

# GitHub CLI Configuration
# Get from: https://github.com/settings/tokens
GITHUB_TOKEN=your-github-personal-access-token-here

# =============================================================================
# AWS CONFIGURATION
# =============================================================================

# AWS Profile
# AWS profile to use for Terraform deployment
AWS_PROFILE=terraform-deploy

# Terraform Deploy User Credentials
# For the terraform-deploy IAM user created during AWS setup
# Note: General AWS access uses AWS CLI profiles (e.g., aws --profile terraform-deploy)
# Uncomment if using access keys directly (not recommended - prefer AWS profiles)
# TERRAFORM_AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
# TERRAFORM_AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY

# =============================================================================
# BACKEND CONFIGURATION
# =============================================================================

# Backend Server Settings
BACKEND_PORT=8000
BACKEND_ENV=production
BACKEND_DEBUG=false

# Backend Security
# Generate with: openssl rand -base64 32
BACKEND_SECRET_KEY=your-secret-key-here-min-32-characters

# CORS Configuration
# Comma-separated list or JSON array of allowed origins
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:5173"]

# =============================================================================
# FRONTEND CONFIGURATION
# =============================================================================

# Vite Frontend Settings
VITE_API_URL=http://localhost:8000
VITE_APP_TITLE="Durable Code Test"

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# PostgreSQL
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Redis Cache
# Format: redis://[:password@]host:port/database
REDIS_URL=redis://localhost:6379/0

# =============================================================================
# DOCKER CONFIGURATION
# =============================================================================

# Docker Compose Settings
COMPOSE_PROJECT_NAME=durable-code
DOCKER_BUILDKIT=1
COMPOSE_DOCKER_CLI_BUILD=1

# =============================================================================
# NOTES
# =============================================================================
#
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in real values for your environment
# 3. NEVER commit .env to version control
# 4. Keep .env.example up to date when adding variables
# 5. Use SCREAMING_SNAKE_CASE for all variable names
# 6. Add comments explaining non-obvious variables
#
# =============================================================================
