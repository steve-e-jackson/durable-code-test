FROM nginx:alpine

# Copy the HTML file
COPY simple-frontend.html /usr/share/nginx/html/index.html

# Copy nginx configuration
COPY simple-nginx.conf /etc/nginx/conf.d/default.conf

# Expose port 3000 (matching ECS configuration)
EXPOSE 3000

# Start nginx
CMD ["nginx", "-g", "daemon off;"]
